import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certify-yaself.snapaday',
  appName: 'snapaday',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
