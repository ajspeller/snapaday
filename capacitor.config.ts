import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certifyyaself.snapaday',
  appName: 'snapaday',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
